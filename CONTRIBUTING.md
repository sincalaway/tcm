# 贡献与部署指南

感谢你为**本草经方**贡献代码、文献索引、测试或文档。本项目是面向中医本草、经方和古籍文本研读的学习型应用，重点在于可追溯的资料整理、阅读线索和个人学习记录。它**不提供诊断、处方、剂量或自行用药建议**；任何贡献都必须保持这一边界。

> 对涉及《伤寒论》方剂、药材、计量和版本异文的内容，项目只展示已说明来源与状态的学习索引。未逐字核对的传本不得标记为确定异文；古今衡重换算只用于文献口径对照，不得改写为临床剂量推荐。

## 1. 技术架构与贡献范围

项目采用 TypeScript 全栈架构。前端通过 React 和 Wouter 构建页面与路由，后端以 Express 和 tRPC 提供类型安全接口，数据层使用 Drizzle ORM 对接 MySQL/TiDB。认证、对象存储、内置托管和可选 AI 服务由现有模板能力提供。

| 区域 | 主要目录 | 贡献时的职责 |
| --- | --- | --- |
| 前端页面 | `client/src/pages/` | 维护本草、经方、古籍、书案等页面及其加载、空状态、错误状态和响应式行为。 |
| 可复用组件 | `client/src/components/` | 抽取有明确复用价值的交互；保留键盘可达性和可见焦点。 |
| 前端工具 | `client/src/lib/` | 放置纯函数、导出格式化、计量换算等可单独测试的逻辑。 |
| 路由契约 | `server/routers/` | 通过 tRPC 输入校验暴露公开或受保护功能；不要创建平行 REST 客户端。 |
| 数据访问 | `server/db.ts` | 在这里实现归属校验、查询和写入；始终按当前用户 ID 限制个人数据。 |
| 目录种子 | `server/catalogSeed.ts`、`server/herbCatalog.ts` | 维护来源可追溯的本草、经方、古籍、条文和版本参照数据。 |
| 数据库模式 | `drizzle/schema.ts` | 修改持久化模型时同步生成迁移，并复核迁移 SQL。 |
| 测试 | `server/**/*.test.ts` | 为纯逻辑、路由权限、目录约束和回归行为补充 Vitest 测试。 |

除非任务明确需要扩展基础设施，请不要直接修改 `server/_core/`、认证底层、构建工具或锁文件。对新功能，优先采用“数据库模式（如需要）→ `server/db.ts` → tRPC 路由 → 前端页面 → 测试”的顺序。

## 2. 本地开发

请使用 Node.js 22+ 与 pnpm 10。仓库的 `packageManager` 字段锁定了 pnpm 版本；应优先使用 Corepack 或与项目兼容的 pnpm，而不是将依赖改为 npm 或 yarn。首次运行请在仓库根目录执行以下命令。

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm dev
```

开发服务器以 `tsx watch` 启动 Express/Vite 集成入口。默认功能依赖 `DATABASE_URL`；当数据库不可用时，类型检查和大部分纯逻辑测试仍可运行，但目录页面不会返回种子内容，受保护的书案功能也不能进行人工验收。

| 场景 | 必需配置 | 说明 |
| --- | --- | --- |
| 本地目录与个人学习功能 | `DATABASE_URL` | MySQL/TiDB 连接字符串。不要提交包含真实凭据的 `.env` 文件。 |
| 登录和会话 | `JWT_SECRET`、OAuth 相关环境变量 | 在受管环境中由平台注入；本地调试应使用安全的开发配置。 |
| 对象存储知识库 | 平台对象存储配置 | 文件本体存对象存储，数据库只保存元数据、文本预览和对象键。 |
| 可选兼容模型端点 | `LOCAL_LLM_*` 或 `NETWORK_LLM_*` | 只有同时提供地址和模型名时才启用；密钥必须通过秘密管理注入。 |

环境变量名称与默认行为以 `server/_core/env.ts`、项目平台配置和 `README.md` 为准。不要在客户端源代码中放置秘密、数据库连接字符串、外部 API 密钥或真实用户数据。

## 3. 数据库与目录初始化

目录数据在第一次目录查询时由既有初始化逻辑写入。种子数据不应被当作替代品覆盖用户生成的收藏、笔记、进度、目标或提醒。任何新增表、字段、索引或约束都需要经过迁移流程。

```bash
# 修改 drizzle/schema.ts 后生成并应用迁移
pnpm db:push
```

提交迁移前，请先读取生成的 SQL，确认不会意外删除表、截断个人数据或放宽用户归属约束。生产迁移应在备份、变更评审和可回滚方案准备完成后执行。若贡献只修改 `catalogSeed.ts`、`herbCatalog.ts` 或前端学习索引，不需要创建数据库迁移。

## 4. 文献与数据贡献规范

本项目将内容准确性、出处透明和学习边界置于文本丰富度之前。提交药材、经方、条文、版本参照或医家研读索引时，必须说明来源、来源 URL、记录状态和使用边界。受版权或使用条款限制的标准正文不得复制到站内；应保留最小化学习索引与原站跳转入口。

| 数据类型 | 最低要求 | 禁止做法 |
| --- | --- | --- |
| 药材 | 稳定 slug、名称、别名、性味归经等学习字段、目录来源入口。 | 以未核验网络内容替换规范化名称；提供个体化用药指令。 |
| 经方 | 方名、出处、组成、学习性结构提示和原典链接。 | 将学习提示写成治疗推荐或处方替代品。 |
| 《伤寒论》条文 | 篇章、站内条文序号、必要摘录、检索词、来源标记与 URL。 | 擅自扩写原文、隐去版本状态或把摘录当定本全文。 |
| 版本参照 | 版本标签、文本参照、核验状态、说明、来源名称和 URL。 | 未逐字校勘却标记为 `verified`；由不同版本推定临床含义。 |
| 衡重换算 | 可选研究口径、计算公式、来源 URL 与明显学习提示。 | 将通用换算器包装成剂量推荐，或把枚、把等非标准单位无条件折算为克。 |

修改目录种子后，至少运行 `pnpm test`。目录测试会检查 slug 唯一性、核心本草数量、必要字段、方剂药味可检索性、条文映射和版本状态。需要新增研究出处时，请将关键核验发现写入 `research/` 下的简短 Markdown 记录，便于审阅者追溯选择依据。

## 5. 个人书案、隐私与导出

收藏、笔记、阅读进度、学习目标、复习提醒和知识库文件属于用户专属数据。后端查询、更新和删除必须始终携带当前认证用户的 ID；不得仅依赖前端隐藏按钮，也不得允许调用方通过 ID 读取其他用户内容。

个人书案支持将当前登录用户的收藏、笔记与阅读进度导出为 Markdown 或 JSON。导出在浏览器本地生成下载文件，不上传至第三方服务，也不会自动共享。贡献者新增可导出的个人数据时，应同步更新导出格式、测试和使用说明；删除或重命名字段时，应考虑现有导出文件的兼容性。

## 6. 代码质量与测试

提交前请在仓库根目录执行完整验证。不要仅运行单个页面，也不要忽略 TypeScript 错误或测试失败。

```bash
pnpm check
pnpm test
pnpm build
```

`pnpm check` 执行 TypeScript 静态检查；`pnpm test` 执行 Vitest 回归套件；`pnpm build` 同时构建浏览器资源与生产服务端入口。仅格式化你实际修改的文件，避免对压缩或历史格式化风格不同的大文件产生无关 diff。

新增功能至少应覆盖以下一项：纯函数的输入输出、tRPC 输入校验、用户归属约束、目录数据完整性、导出文件内容或关键空状态。涉及 UI 的改动还应手动确认键盘焦点、窄屏布局、加载状态、错误状态和 `prefers-reduced-motion` 行为。

## 7. 提交与拉取请求

请从最新 `main` 分支创建语义清晰的分支。提交信息建议使用 `feat:`、`fix:`、`docs:`、`test:`、`refactor:` 或 `chore:` 前缀，并以简短中文说明主要变更，例如：`feat: 增加书案本地导出`。

拉取请求正文应以完整段落说明问题、方案、数据或权限影响和验证结果；若涉及页面，请附上桌面与移动端截图或说明无法完成浏览器验收的原因。涉及古籍、计量或药材数据时，正文还应列出来源 URL 与核验状态。避免将“更多内容”作为合并理由；应清楚解释新增内容为什么适合本项目的学习边界。

| 检查项 | 合并前要求 |
| --- | --- |
| 功能范围 | 变更与 issue、需求或文档目标一致，没有无关重构。 |
| 类型与测试 | `pnpm check` 与 `pnpm test` 通过；新增风险有相应回归测试。 |
| 构建 | `pnpm build` 通过，且未引入未解释的构建错误。 |
| 数据与来源 | 结构化目录记录完整，来源可打开，状态标签诚实。 |
| 隐私与安全 | 用户数据按归属隔离；没有提交秘密、个人资料或测试账户内容。 |
| 产品边界 | 页面与文案不构成诊断、处方、剂量或自行用药建议。 |

## 8. 部署流程

项目使用内置托管能力。保存新的项目版本后，受管环境会完成构建和发布；不需要额外配置第三方持续部署服务。发布前应在预览环境检查公开检索、本草与经方详情、古籍阅读、登录后的书案和导出入口。

生产发布应遵循以下顺序：先在受控环境配置秘密与数据库连接；再应用经审阅的迁移；随后构建并运行测试；最后发布新版本并进行最小化冒烟验证。若发布涉及目录种子，首次访问目录后确认种子初始化没有影响用户数据。若发布涉及认证、定时提醒或对象存储，额外确认 OAuth 回调、计划任务和签名下载路径正常。

```bash
# 发布前的最小验证
pnpm install --frozen-lockfile
pnpm check
pnpm test
pnpm build

# 已在安全环境配置 DATABASE_URL 后，按需要执行
pnpm db:push

# 自托管生产进程的构建后启动方式
NODE_ENV=production pnpm start
```

自托管时，应用必须运行在能够安全提供会话 cookie、数据库访问、对象存储访问和 HTTPS 终止的环境中。请为数据库执行备份与恢复演练，为日志设定访问控制，并将秘密交由部署平台或秘密管理服务注入。不要以仓库中的静态 `.env` 文件替代生产秘密管理。

## 9. 安全问题与行为准则

请不要在公开 issue 中披露漏洞利用细节、访问令牌、会话信息、数据库导出或任何用户笔记内容。安全问题应私下通知仓库维护者，并提供最小可复现步骤、影响范围和建议修复方式。维护者会确认收到报告、评估影响并协调修复与披露时间。

社区讨论应围绕代码、资料与可验证证据展开。欢迎纠正史料、版本、术语和实现问题；请避免将学术立场转化为对个人的攻击。对不确定内容，优先标记“待核”并提供来源，而不是以确定语气写入产品数据。

## 10. 参考资料

项目数据与学习索引依赖公开原典、目录入口和研究资料。贡献者可从下列入口开始核验，但仍应针对具体记录保留直接出处。

1. [《伤寒论》原典页，中文维基文库](https://zh.wikisource.org/zh-hans/%E5%82%B7%E5%AF%92%E8%AB%96)
2. [中华人民共和国药典在线](https://ydz.chp.org.cn/)
3. [《伤寒论》中“两”如何换算为现今之“克”](https://www.nricm.edu.tw/var/file/0/1000/attach/73/pta_2250_6508780_30566.pdf)
4. [《仲景方用药剂量的古今折算与临床应用》](http://xbsk.njucm.edu.cn/cn/article/pdf/preview/SK2014_0306.pdf)
